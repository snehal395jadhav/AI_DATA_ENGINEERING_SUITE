# ── Core app ───────────────────────────────────────────────
streamlit>=1.32.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.18.0
scipy>=1.11.0
requests>=2.31.0
python-dotenv>=1.0.0

# ── ML analytics (predictive + anomaly detection) ──────────
scikit-learn>=1.3.0
xgboost>=2.0.0

# ── Vector database (semantic search / RAG) ────────────────
chromadb>=0.4.0

# ── Agentic orchestration ──────────────────────────────────
langgraph>=0.2.0
langchain>=0.2.0

# NOTE: chromadb, xgboost, langgraph & langchain are optional —
# the app auto-falls back to lightweight built-ins if any are
# missing, so it still runs end-to-end without them.
